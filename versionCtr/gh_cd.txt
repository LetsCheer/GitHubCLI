Here's your complete GitHub CLI Cheat Sheet — a single index.html file ready to run in any browser.
What's inside:

1. 20 categories covering every command group from the official manual — auth, repo, pr, issue, workflow, run, release, gist, secret, codespace, search, and more
100+ commands each with a title, description, full syntax, flags table, and practical examples
2. Copy buttons on every example code block (hover to reveal)
Command Palette (⌘K) — fuzzy search across all commands with keyboard navigation (↑↓ + Enter)
3. Sidebar filter — real-time filtering with instant show/hide of categories and commands
4. Scroll spy — active nav item highlights as you scroll
5. Collapsible sections — click any category header to collapse/expand
6. Mobile drawer — hamburger menu toggles the sidebar on small screens
7. Glassmorphism dark theme — custom color-coded per category (auth = blue, issues = red, workflows = green, etc.)